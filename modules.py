# import random
# from random import random as random
# python -m pip install package-name
# auto-pep8 to format code
# help(package-name)
# import builtins
# dir(package-name//builtins)
# __name__ = __main__ for the file being executed else it's name of the file
# when you import, python also runs the file code, so you may want it to execute code
# under condition id __name__ == '__main__'

import function

print(function.estimate_pi(1000000))
